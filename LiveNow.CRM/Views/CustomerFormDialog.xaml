<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="LiveNow.CRM.Views.CustomerFormDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Nuevo cliente"
    PrimaryButtonText="Guardar"
    CloseButtonText="Cancelar"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <ScrollViewer MaxHeight="500">
        <StackPanel Spacing="12" Padding="0,8,0,0">
            <InfoBar x:Name="ValidationInfoBar" IsOpen="False" Severity="Error" Title="Error de validación" />
            <TextBox x:Name="FirstNameTextBox" Header="Nombre *" PlaceholderText="Ingrese el nombre" />
            <TextBox x:Name="LastNameTextBox" Header="Apellido *" PlaceholderText="Ingrese el apellido" />
            <TextBox x:Name="EmailTextBox" Header="Email" PlaceholderText="correo@ejemplo.com" />
            <TextBox x:Name="PhoneTextBox" Header="Teléfono" PlaceholderText="+1 234 567 890" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="CountryTextBox" Header="País" PlaceholderText="País" Margin="0,0,4,0" />
                <TextBox Grid.Column="1" x:Name="CityTextBox" Header="Ciudad" PlaceholderText="Ciudad" Margin="4,0,0,0" />
            </Grid>
            <DatePicker x:Name="DateOfBirthDatePicker" Header="Fecha de nacimiento" />
            <TextBox x:Name="NotesTextBox" Header="Notas" PlaceholderText="Notas adicionales" AcceptsReturn="True" TextWrapping="Wrap" Height="80" />
            <ComboBox x:Name="StatusComboBox" Header="Estado" SelectedIndex="0" HorizontalAlignment="Stretch">
                <x:String>Active</x:String>
                <x:String>Inactive</x:String>
                <x:String>Blocked</x:String>
            </ComboBox>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
