<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LiveNow.CRM.Views.CustomersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Clientes" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Nuevo cliente" Click="NewCustomerButton_Click" Style="{StaticResource AccentButtonStyle}" />
        </Grid>
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Buscar por nombre, apellido, email o teléfono..." Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center">
                <TextBox.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Enter" Invoked="SearchBox_Invoked" />
                </TextBox.KeyboardAccelerators>
            </TextBox>
            <Button Grid.Column="1" Content="Buscar" Margin="8,0,0,0" Click="SearchButton_Click" VerticalAlignment="Center" />
            <Button Grid.Column="2" Content="Actualizar" Margin="8,0,0,0" Click="RefreshButton_Click" VerticalAlignment="Center" />
        </Grid>
        <InfoBar Grid.Row="2" IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}" Severity="Error" Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Margin="0,0,0,12" CloseButtonClick="InfoBar_CloseButtonClick" />
        <InfoBar Grid.Row="2" x:Name="SuccessInfoBar" IsOpen="False" Severity="Success" Title="Operación exitosa" Message="{x:Bind SuccessMessage, Mode=OneWay}" Margin="0,0,0,12" />
        <ProgressRing Grid.Row="3" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Height="50" />
        <ListView Grid.Row="3" ItemsSource="{x:Bind ViewModel.Customers, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedCustomer, Mode=TwoWay}" SelectionMode="Single" IsItemClickEnabled="True" ItemClick="CustomersList_ItemClick" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontWeight="SemiBold" FontSize="16">
                                <Run Text="{Binding FirstName}" />
                                <Run Text="{Binding LastName}" />
                            </TextBlock>
                            <TextBlock Text="{Binding Email}" Opacity="0.7" FontSize="13" />
                            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,4,0,0">
                                <TextBlock FontSize="12" Opacity="0.6"><Run Text="Tel: " /><Run Text="{Binding Phone}" /></TextBlock>
                                <TextBlock FontSize="12" Opacity="0.6"><Run Text="Estado: " /><Run Text="{Binding Status}" /></TextBlock>
                                <TextBlock FontSize="12" Opacity="0.6"><Run Text="Creado: " /><Run Text="{Binding CreatedAt}" /></TextBlock>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Editar" Tag="{Binding Id}" Click="EditButton_Click" />
                            <Button Content="Eliminar" Tag="{Binding Id}" Click="DeleteButton_Click" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Bind ViewModel.PageInfo, Mode=OneWay}" VerticalAlignment="Center" Opacity="0.7" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                <Button Content="⏮" Click="FirstPageButton_Click" IsEnabled="{x:Bind ViewModel.HasPreviousPage, Mode=OneWay}" ToolTipService.ToolTip="Primera página" />
                <Button Content="◀" Click="PreviousPageButton_Click" IsEnabled="{x:Bind ViewModel.HasPreviousPage, Mode=OneWay}" ToolTipService.ToolTip="Página anterior" />
                <Button Content="▶" Click="NextPageButton_Click" IsEnabled="{x:Bind ViewModel.HasNextPage, Mode=OneWay}" ToolTipService.ToolTip="Página siguiente" />
                <Button Content="⏭" Click="LastPageButton_Click" IsEnabled="{x:Bind ViewModel.HasNextPage, Mode=OneWay}" ToolTipService.ToolTip="Última página" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>

