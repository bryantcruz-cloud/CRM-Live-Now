<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="LiveNow.CRM.Views.PaymentsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Pagos y finanzas" FontSize="28" FontWeight="SemiBold"/><Button Grid.Column="1" Content="Registrar pago" Click="RegisterPayment_Click" Style="{StaticResource AccentButtonStyle}"/></Grid>
        <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="3*"/></Grid.ColumnDefinitions>
            <ListView Grid.Column="0" ItemsSource="{x:Bind ViewModel.Sales}" SelectedItem="{x:Bind ViewModel.SelectedSale, Mode=TwoWay}" SelectionChanged="Sale_SelectionChanged"><ListView.ItemTemplate><DataTemplate><StackPanel Padding="8"><TextBlock Text="{Binding SaleNumber}" FontWeight="SemiBold"/><TextBlock Text="{Binding CustomerName}"/><TextBlock Text="{Binding RaceEditionName}"/><TextBlock Text="{Binding Status}" Opacity="0.7"/><TextBlock Text="{Binding TotalSalePrice}"/></StackPanel></DataTemplate></ListView.ItemTemplate></ListView>
            <Grid Grid.Column="1" Margin="16,0,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <StackPanel x:Name="SummaryPanel" Orientation="Horizontal" Spacing="18" Margin="0,0,0,16"><TextBlock x:Name="SaleTotalText"/><TextBlock x:Name="PaidText"/><TextBlock x:Name="OutstandingText"/><TextBlock x:Name="FeesText"/><TextBlock x:Name="ProfitText"/></StackPanel>
                <TextBlock Grid.Row="1" Text="Historial de pagos" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Payments}"><ListView.ItemTemplate><DataTemplate><Grid Padding="6"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding PaymentDate}"/><TextBlock Grid.Column="1" Text="{Binding PaymentMethod}" Margin="12,0"/><TextBlock Grid.Column="2" Text="{Binding Amount}"/></Grid></DataTemplate></ListView.ItemTemplate></ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>
