<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="LiveNow.CRM.Views.RegistrationsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Margin="20"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Grid Grid.Row="0" Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="Registros de corredores" FontSize="28" FontWeight="SemiBold"/><Button Grid.Column="1" Content="Editar seleccionado" Click="Edit_Click" Margin="8,0"/><Button Grid.Column="2" Content="Nuevo registro" Click="New_Click" Style="{StaticResource AccentButtonStyle}"/></Grid>
  <Grid Grid.Row="1" Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}" PlaceholderText="Buscar por cliente o plaza"/><ComboBox Grid.Column="1" Margin="8,0,0,0" ItemsSource="{x:Bind ViewModel.StatusOptions}" SelectedItem="{x:Bind ViewModel.StatusFilter, Mode=TwoWay}" PlaceholderText="Todos" MinWidth="160"/></Grid>
   <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.FilteredRegistrations}" SelectedItem="{x:Bind SelectedRegistration, Mode=TwoWay}"><ListView.ItemTemplate><DataTemplate><Grid Padding="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding CustomerName}" FontWeight="SemiBold"/><TextBlock Text="{Binding RaceEditionName}"/><TextBlock Text="{Binding RaceSlotCode}" Opacity="0.7"/></StackPanel><TextBlock Grid.Column="1" Text="{Binding RegistrationStatus}" VerticalAlignment="Center"/></Grid></DataTemplate></ListView.ItemTemplate></ListView>
 </Grid>
</Page>
