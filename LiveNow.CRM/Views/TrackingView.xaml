<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="LiveNow.CRM.Views.TrackingView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20" RowSpacing="12"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Seguimiento y checklist" FontSize="28" FontWeight="SemiBold" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12"><ComboBox Width="320" Header="Cliente" ItemsSource="{x:Bind ViewModel.Customers}" SelectedItem="{x:Bind ViewModel.SelectedCustomer, Mode=TwoWay}" DisplayMemberPath="DisplayName" SelectionChanged="Customer_Changed"/><TextBlock Text="Pendientes:" VerticalAlignment="Bottom" FontSize="16"/><TextBlock Text="{x:Bind ViewModel.PendingCount, Mode=OneWay}" VerticalAlignment="Bottom" FontSize="16" FontWeight="Bold"/></StackPanel>
        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Items}" SelectionMode="None"><ListView.ItemTemplate><DataTemplate><Grid Padding="8" ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions><CheckBox IsChecked="{Binding IsCompleted, Mode=TwoWay}" Click="Checklist_Click" VerticalAlignment="Center"/><StackPanel Grid.Column="1"><TextBlock Text="{Binding Description}" FontWeight="SemiBold"/><TextBlock Text="{Binding ItemType}" Opacity="0.7"/><TextBlock Text="{Binding Notes}" Opacity="0.7" TextWrapping="Wrap"/></StackPanel><TextBlock Grid.Column="2" Text="{Binding CompletedAt}" HorizontalAlignment="Right" VerticalAlignment="Center"/></Grid></DataTemplate></ListView.ItemTemplate></ListView>
    </Grid>
</Page>
